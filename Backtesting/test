from bokeh.models import DatetimeTickFormatter

bt_result = bt.run()
figure = bt_result.plot()  # This returns a Bokeh figure object

# Adjusting the date format manually
figure.xaxis.formatter = DatetimeTickFormatter(
    days="%d %b",
    months="%b %Y",
    years="%Y"
)
